<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.ufjf.dcc196.hunterapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.AtividadeDetalheActivity"></activity>
        <activity android:name=".Activities.AtividadeNovoActivity" />
        <activity android:name=".Activities.AtividadeActivity" />
        <activity android:name=".Activities.ProducaoDetalheActivity" />
        <activity android:name=".Activities.ProducaoNovoActivity" />
        <activity android:name=".Activities.ProducaoActivity" />
        <activity android:name=".Activities.CandidatoDetalheActivity" />
        <activity android:name=".Activities.CandidatoNovoActivity" />
        <activity android:name=".Activities.CandidatoActivity" />
        <activity android:name=".Activities.CategoriaNovoActivity" />
        <activity android:name=".Activities.CategoriaDetalheActivity" />
        <activity android:name=".Activities.CategoriaActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>